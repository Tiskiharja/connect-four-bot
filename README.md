# Connect Four Bot

Playground entrypoint:

```python
def make_move(board, player):
    ...
```

Paste `main.py` into the playground when testing the bot.

Run local checks:

```bash
python3 -m unittest discover -s tests
python3 harness.py --games 20 --seed 1
```
